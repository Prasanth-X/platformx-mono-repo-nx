import PageScroll from '../Common/PageScroll';

const QuizPageScroll = ({ icons, parentToolTip, srollToView }) => {
  return (
    <PageScroll
      icons={icons}
      parentToolTip={parentToolTip}
      srollToView={srollToView}
    />
  );
};
export default QuizPageScroll;
