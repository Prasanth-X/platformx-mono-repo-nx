import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  recentContainer: {
    overflowY: 'auto',
    maxHeight: '305px',
    minHeight: '305px',
    padding: '0px 24px',
  },
}));
